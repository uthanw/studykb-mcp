## 知识库 + 进度追踪 MCP 协议设计

### 项目背景

为考研学习场景设计一套 MCP（Model Context Protocol）工具集，实现：
1. 知识库管理：多学科教材资料的存储、索引、检索
2. 进度追踪：基于艾宾浩斯遗忘曲线的学习状态管理
3. Agent 友好：返回格式模拟交互式 TUI，信息密度高且 token 开销小
4. sse+streamablehttp

---


### 知识库结构

```
/kb/                                    ← 知识库根目录
├── 数据结构/                           ← 大类 (Category)
│   ├── 王道数据结构.md                 ← 资料文件 (Material)
│   ├── 王道数据结构_index.md           ← 索引文件 (可选)
│   ├── 天勤数据结构.md
│   └── 算法全景图.md
├── 计算机组成原理/
│   ├── 王道计组.md
│   └── 王道计组_index.md
├── 有机化学/
│   └── 有机化学教材.md
└── ...

/progress/                              ← 进度数据目录
├── 数据结构.json                       ← 按大类存储进度
├── 计算机组成原理.json
└── ...
```

**索引文件命名规则**：资料文件名 + `_index` 后缀，如 `王道数据结构_index.md`

**索引文件内容示例**：
```markdown
# 王道《数据结构》章节索引

## 📚 文件结构概览
| 部分 | 行号范围 | 说明 |
|------|---------|------|
| 前置内容 | 1-311 | 封面、版权、前言 |
| 目录 | 312-730 | 全书目录 |
| 正文 | 732-23923 | 8章正文内容 |

## 第1章 绪论（732-1453行）
| 行号范围 | 内容 |
|---------|------|
| 762-942 | **1.1 数据结构的基本概念** |
| 943-1409 | **1.2 算法和算法评价** |
| 975-1028 | └── 时间复杂度 |

## 🔍 快速查找
| 知识点 | 行号 | 章节 |
|-------|------|------|
| Kruskal算法 | 14230-14280 | 6.4.1 |
| 快速排序 | 20741-20856 | 8.3.2 |
```

---

### 进度数据结构

```json
{
  "category": "数据结构",
  "last_updated": "2025-01-20T14:30:00",
  "entries": {
    "ds.graph.mst.kruskal": {
      "name": "Kruskal算法",
      "status": "done",
      "comment": "边排序+并查集，时间O(ElogE)",
      "updated_at": "2025-01-20T14:30:00",
      "mastered_at": "2025-01-20T14:30:00",
      "review_count": 0,
      "next_review_at": "2025-01-27T00:00:00"
    }
  }
}
```

**状态定义**：
- `active` - 正在学习
- `done` - 已掌握（触发复习计时）
- `review` - 需要复习（由系统自动从 done 降级）
- `pending` - 待学习（可选，用于规划）

**复习机制**：基于艾宾浩斯遗忘曲线自动衰减
- 首次标记 done 后 7 天触发 review
- 每次成功复习，间隔 × 1.5
- 最长间隔不超过 90 天
- `read_progress` 调用时自动检查并更新过期的 `done` → `review`

---

### MCP Tools 定义

#### Tool 1: `read_overview`

```yaml
name: read_overview
description: |
  获取知识库全景图，列出所有大类及其包含的资料文件。

  📌 调用时机：
  - 对话开始时，了解当前知识库有哪些内容
  - 用户提到一个你不确定是否存在的学科/资料时
  - 需要向用户展示可学习的范围时

  ⚠️ 注意：
  - 这是轻量级调用，返回概览信息，不包含具体内容
  - 确认资料存在后，再用 read_index 或 grep 深入

parameters: 无
```

**返回格式**：
```
# Knowledge Base Overview

数据结构/ (3 files)
  ├ 王道数据结构         23965 ln  [IDX]
  ├ 天勤数据结构         18420 ln
  └ 算法全景图            3500 ln  [IDX]

计算机组成原理/ (2 files)
  ├ 王道计组             19800 ln  [IDX]
  └ 唐朔飞计组           15600 ln

有机化学/ (2 files)
  ├ 有机化学教材          8900 ln
  └ 有机反应机理总结      2100 ln

---
5 categories, 11 files
[IDX] = index available
```

---

#### Tool 2: `read_progress`

```yaml
name: read_progress
description: |
  获取某个大类的学习进度追踪数据。

  📌 调用时机：
  - 用户说"继续学习""今天学什么"时，了解当前进度
  - 需要确定下一个学习内容时
  - 用户问"我学到哪了""还有多少没学"时
  - 检查是否有需要复习的内容时

  🔗 推荐前置调用：
  - read_overview：确认大类名称存在

  🔗 推荐后续调用：
  - read_index / grep：根据进度定位具体内容
  - update_progress：开始新知识点时标记 active

parameters:
  category:
    type: string
    required: true
    description: 大类名称，如 "数据结构"、"计算机组成原理"

  status_filter:
    type: array[string]
    required: false
    default: null (返回全部状态)
    description: |
      筛选状态，可选值: "done", "active", "review", "pending"
      示例: ["active", "review"] 只返回进行中和待复习的

  since:
    type: string
    required: false
    default: "all"
    description: |
      时间范围筛选，基于 updated_at
      可选值: "7d", "30d", "90d", "all"

  limit:
    type: integer
    required: false
    default: 20
    description: |
      每个状态分组的最大返回条数
      设为 -1 返回全部
```

**返回格式（完整视图）**：
```
# Progress: 数据结构

done: 23 | active: 1 | review: 3 | pending: 18 | total: 45
[====================............ ] 51%

## 🔥 active (1)

ds.graph.mst.kruskal | Kruskal算法
  "边排序+并查集思路，正在看具体实现"
  updated: 2h ago

## 🔄 review (3)

ds.linear.array.prefix_sum | 前缀和 | +14d overdue
  "一维二维都搞定了"

ds.tree.traversal.inorder | 中序遍历 | +10d overdue
  "递归秒杀，迭代用栈模拟"

ds.sort.quick_sort | 快速排序 | +8d overdue
  "partition写法要熟练"

## ✅ done (showing 5, +18 more)

ds.graph.mst.prim         | Prim算法      | Jan 19
ds.graph.shortest.dijkstra | Dijkstra     | Jan 18
ds.graph.traversal.bfs    | BFS          | Jan 17
ds.graph.traversal.dfs    | DFS          | Jan 17
ds.graph.storage.adj_list | 邻接表        | Jan 16

---
Showing 5 of 23. Use limit=-1 to fetch all.
```

**返回格式（筛选视图）**：
```
# Progress: 数据结构 [filter: review]

## 🔄 review (3)

ds.linear.array.prefix_sum | 前缀和
  +14d overdue | reviewed 2x
  "一维二维都搞定了"

ds.tree.traversal.inorder | 中序遍历
  +10d overdue | reviewed 1x
  "递归秒杀，迭代用栈模拟"

ds.sort.quick_sort | 快速排序
  +8d overdue | reviewed 0x
  "partition写法要熟练"
```

---

#### Tool 3: `update_progress`

```yaml
name: update_progress
description: |
  创建或更新学习进度条目。

  📌 调用时机：
  - 开始学习新知识点时 → status="active"
  - 用户表示掌握了某个知识点时 → status="done"
  - 用户说"这个要复习"或完成复习时 → status="review" / "done"
  - 用户更新对某个知识点的理解/笔记时 → 更新 comment

  🔗 推荐前置调用：
  - read_progress：确认当前状态，避免重复创建

  ⚠️ 注意：
  - progress_id 使用点分格式，如 "ds.graph.mst.kruskal"
  - 首次创建时 name 必填，后续更新可省略
  - status 变为 done 时会自动设置 next_review 时间

parameters:
  category:
    type: string
    required: true
    description: 大类名称

  progress_id:
    type: string
    required: true
    description: |
      进度标识，使用点分层级格式
      示例: "ds.graph.mst.kruskal", "ds.dp.knapsack.01"

  name:
    type: string
    required: false (首次创建时必填)
    description: 知识点名称，如 "Kruskal算法"

  status:
    type: string
    required: true
    enum: ["active", "done", "review"]
    description: |
      active: 正在学习
      done: 已掌握（会触发复习计时）
      review: 需要复习

  comment:
    type: string
    required: true
    description: 一句话描述当前理解/进度/笔记
```

**返回格式（更新）**：
```
✅ Progress updated

数据结构 / ds.graph.mst.kruskal
Kruskal算法

  active → done
  "边排序+并查集，时间O(ElogE)，空间O(V)"

next review: Jan 27 (7d)
```

**返回格式（新建）**：
```
✨ Progress created

数据结构 / ds.dp.knapsack.01
0-1背包问题 [NEW]

  status: active
  "开始学习，先理解状态定义"
```

---

#### Tool 4: `read_index`

```yaml
name: read_index
description: |
  读取资料的索引文件，获取章节结构和行号映射。

  📌 调用时机：
  - 需要定位某个知识点在资料中的具体位置时
  - 准备用 read_file 读取内容前，先查行号
  - 用户问"这个在书的哪里"时

  🔗 推荐前置调用：
  - read_overview：确认资料存在且有 [IDX] 标记

  🔗 推荐后续调用：
  - read_file：根据索引中的行号读取具体内容

  ⚠️ 注意：
  - 只有标记 [IDX] 的资料才有索引文件
  - 无索引时返回错误提示，建议改用 grep
  - 索引内容完整返回，可能较长

parameters:
  category:
    type: string
    required: true
    description: 大类名称

  material:
    type: string
    required: true
    description: 资料名称（不含扩展名），如 "王道数据结构"
```

**返回格式**：索引文件的完整 Markdown 内容

**无索引时**：
```
⚠️ Index not found: 数据结构/天勤数据结构

No index file available for this material.
Use `grep` to search or `read_file` with estimated ranges.
```

---

#### Tool 5: `read_file`

```yaml
name: read_file
description: |
  读取资料文件的指定行范围内容。

  📌 调用时机：
  - 通过 read_index 或 grep 定位到行号后，读取原文
  - 需要给用户展示/讲解教材具体内容时
  - 用户说"给我看看原文""书上怎么说的"时

  🔗 推荐前置调用：
  - read_index：获取准确的行号范围
  - grep：搜索关键词定位行号

  ⚠️ 注意：
  - 单次最多读取 500 行，超出会截断并提示
  - 行号从 1 开始计数
  - 建议精确定位后读取，避免读太多无关内容

parameters:
  category:
    type: string
    required: true
    description: 大类名称

  material:
    type: string
    required: true
    description: 资料名称（不含扩展名）

  start_line:
    type: integer
    required: true
    description: 起始行号（包含）

  end_line:
    type: integer
    required: true
    description: 结束行号（包含）
```

**返回格式**：
```
# 数据结构/王道数据结构 L14230-14260

14230 | **2. Kruskal 算法**
14231 | 
14232 | Kruskal 算法是一种按权值的递增次序选择合适的边来构造最小
14233 | 生成树的方法。
14234 | 
14235 | 算法思想：设连通网 N=(V,E)，令最小生成树的初始状态为只有
14236 | n 个顶点而无边的非连通图 T=(V,{})...
```

**截断时**：
```
# 数据结构/王道数据结构 L1-600 (truncated at 500 lines)
...
⚠️ Content truncated. Request a smaller range.
```

---

#### Tool 6: `grep`

```yaml
name: grep
description: |
  在资料中搜索关键词，返回匹配行及上下文。

  📌 调用时机：
  - 资料没有索引文件时，用搜索定位内容
  - 索引不够详细，需要精确查找某个术语时
  - 用户问"xxx在哪里提到过"时
  - 需要查找某个概念的所有出现位置时

  🔗 推荐后续调用：
  - read_file：根据搜索结果的行号读取完整段落

  ⚠️ 注意：
  - 不指定 material 时搜索整个大类，可能较慢
  - 匹配结果有上限，超出会截断
  - 支持简单文本匹配，不支持正则

parameters:
  category:
    type: string
    required: true
    description: 大类名称

  material:
    type: string
    required: false
    default: null (搜索整个大类)
    description: 资料名称，不填则搜索该大类下所有文件

  pattern:
    type: string
    required: true
    description: 搜索关键词

  context_lines:
    type: integer
    required: false
    default: 2
    description: 匹配行的上下文行数（上下各 N 行）

  max_matches:
    type: integer
    required: false
    default: 20
    description: |
      最大返回匹配数
      设为 -1 返回全部
```

**返回格式（单文件）**：
```
# grep "Kruskal" in 数据结构/王道数据结构
4 matches

[L14230]
  14228 | 的时间复杂度为 O(ElogE)。
> 14230 | **2. Kruskal 算法**
  14232 | Kruskal 算法是一种按权值的递增次序选择合适的边来构造最小

[L14267]
  14265 | 
> 14267 | 使用 Kruskal 算法求下图的最小生成树。
  14269 | 解：首先将边按权值排序：

[L14289]
  14287 | 【性能分析】
> 14289 | Kruskal 算法的时间复杂度为 O(ElogE)，主要花在边排序上。
  14291 | 适用于边稀疏而顶点较多的图。

[L15102]
  15100 | 13. 下列关于最小生成树的说法，正确的是（  ）
> 15102 | A. Kruskal 算法的时间复杂度与图的顶点数无关
  15104 | C. 最小生成树是唯一的
```

**返回格式（跨文件）**：
```
# grep "并查集" in 数据结构/*
12 matches (showing 3, +9 more)

-- 王道数据结构 (8) --

[L11479]
> 11479 | ## 5.5.2 并查集
  11481 | 并查集（Union-Find Sets）是一种树型数据结构

[L14243]
> 14243 | （1）初始化：将图的边按权值从小到大排序，初始化并查集

... +6 more

-- 算法全景图 (4) --

[L892]
> 892 | │   ├── 并查集 (Union-Find / Disjoint Set) ⭐

... +3 more
```

**无匹配**：
```
# grep "量子计算" in 数据结构/*
0 matches

Try different keywords or search in other categories.
```

---

### 典型交互流程

```
用户: "今天学点新东西"
         │
         ▼
    read_overview()          ← 看看知识库有什么
         │
         ▼
    read_progress(...)       ← 看看学到哪了
         │
         ▼
    ┌────┴────┐
    │ 有索引?  │
    └────┬────┘
      是 │ 否
         │    │
         ▼    ▼
  read_index() grep()        ← 定位内容
         │    │
         └──┬─┘
            ▼
      read_file()            ← 读取原文
            │
            ▼
     update_progress()       ← 标记开始学习
            │
            ▼
        [开始讲解]
```
